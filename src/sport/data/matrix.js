// Scenario × Brand impact matrix. Rows = scenarios (order of SC), columns =
// brands (order of BR). Each cell: { r: "Threat" | "Opportunity" | "Monitor",
// i: 1–4 intensity }.
// Populate only after both scenarios.js and brands.js are stable.

export const MX = [];
