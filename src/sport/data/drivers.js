// Drivers are derived from clustering signals in ./signals.js — do NOT
// populate this file before the scan reaches at least 100 signals.
// Each driver is a force, not a trend (a trend is a signal of a driver).
// Schema mirrors the Mars instance:
//   { id, n (name), ct (signal count), cl (colour), ic (emoji), ds (description) }

export const DR = [];
