// Strategic actions grouped by horizon. NOW = act immediately, MON = monitor
// triggers, PREP = build optionality.
// Each action: { a (action), s (scenario id link), w (deadline / trigger) }

export const TL = { NOW: [], MON: [], PREP: [] };
